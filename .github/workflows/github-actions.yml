name: Deploy discord_bot discord_bot
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - run: docker build -t site_checker .
      - run: docker-compose run -e tg_token=${{ secrets.TG_TOKEN }}dis_token=${{ secrets.DIS_TOKEN }} site_checker -d

